PySide6>=6.0.0
python-mpv>=0.5.2
Send2Trash>=1.8.0
yt-dlp>=2026.8.19

# Portable archive playback note:
# RAR support also needs a bundled archive backend binary at build/runtime,
# preferably vendor/bsdtar.exe (or vendor/tar.exe as fallback).
